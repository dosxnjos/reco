# PyInstaller
dist/
build/
*.spec.tmp
reco_build.spec

# Bundled model (fetched at build time, ~250 MB — not tracked)
models/

# Python cache
__pycache__/
*.pyc
*.pyo

# Local config (paths + preferences)
.reco_config.json
.reco_config.corrupt

# OS
Thumbs.db
desktop.ini

# Scratch
*.log

# logo working files — keep only the app icon + 1x1 png
logo/.claude/
logo/*.png
!logo/logo_symbol_1x1.png
logo/ChatGPT*
